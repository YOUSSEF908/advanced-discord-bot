<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || "Dashboard" %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <div class="profile">
            <img src="https://cdn.discordapp.com/avatars/<%= user.discordId %>/<%= user.avatar %>.webp?dynamic=true" alt="User Avatar" class="profile-pic">
            <span>Welcome, <%= user ? user.username : 'Guest' %></span>
        </div>
        <nav>
            <ul class="nav-list">
                <li><a href="/">Home</a></li>
                <% if (user) { %>
                    <li><a href="/auth/logout">Logout</a></li>
                <% } else { %>
                    <li><a href="/auth/login">Login</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
    <main>